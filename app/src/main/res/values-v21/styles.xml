<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimaryDark">@android:color/holo_blue_dark</item>
        <item name="android:colorPrimary">#33b5e5</item>
        <!--我设置的这个不起作用 具体原因还不明确-->
      <!--  <item name="android:colorAccent">#ff0000</item>-->

        <!--ToolBar是在新版本Android上使用的一个兼容组件，在使用WebView，EditText的时候，
        如果长按进入选入状态，ToolBar不会被弹出的上下文菜单（Contextual Action Bar， CAB）
        覆盖，而是被压下去了，加上下面这两个属性就不会出现问题了-->

        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/Widget.AppCompat.ActionMode</item>
    </style>
</resources>
